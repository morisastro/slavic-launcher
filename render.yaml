services:
  - type: web
    name: slavic-launcher-backend
    runtime: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    healthCheckPath: /api/health
    envVars:
      - key: ADMIN_KEY
        generateValue: true
    autoDeploy: true